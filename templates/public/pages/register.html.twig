{% extends 'public/comp/base.html.twig' %}
{% block body %}
    {% if app.user %}
    {% else %}
        <div class="row justify-content-center">
            <div class="col-md-6">
                <a class="btn btn-info m-4 text-center " href="/login">I have a User - Send me to Login</a>
                {{ form(form) }}
            </div>
        </div>
    {% endif %}
{% endblock %}