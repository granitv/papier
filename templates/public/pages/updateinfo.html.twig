{% extends'public/comp/base.html.twig' %}

{% block body %}
   {# ->add('fullname')
    ->add('country')
    ->add('street')
    ->add('postcode')
    ->add('city')
    ->add('tel')
    ->add('note')
    ->add('save',SubmitType::class,['attr'=>['class'=>'btn btn-success']])#}

    <div class="row justify-content-center">

        <div class="col-md-6">
            {{ form_start(updateinfoForm) }}
            <label>Full Name:</label>
            {{ form_widget(updateinfoForm.fullname) }}
            <label>Street:</label>
            {{ form_widget(updateinfoForm.street) }}
            <label>Postcode:</label>
            {{ form_widget(updateinfoForm.postcode) }}
            <label>City:</label>
            {{ form_widget(updateinfoForm.city) }}
            <label>Tel:</label>
            {{ form_widget(updateinfoForm.tel) }}
            <div class="row justify-content-between p-4">
                <div>
                    <label>Notes:</label>
                    {{ form_widget(updateinfoForm.note) }}
                </div>
            </div>
            {{ form_end(updateinfoForm) }}
        </div>
    </div>
{% endblock %}