{% extends 'admin/comp/base.html.twig' %}
{% block body %}
    <h1>TYpe</h1>
    <table class="table table-bordered">
        <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Name</th>
            <th scope="col">Price</th>
            <th scope="col">Active</th>
            <th scope="col">Edit</th>
            <th scope="col">Delete</th>
        </tr>
        </thead>
        <tbody>
        {% for t in allTypee %}
        <tr>
            <th scope="row">{{ t.id }}</th>
            <td>{{ t.name }}</td>
            <td>{{ t.price }} € m<sup>2</sup></td>
            <td>{% if t.active == 1 %}<span class="alert-success p-2">Active</span>{% else %}<span class="alert-danger p-2">Deactive</span>{% endif %}</td>
            <td><a href="{{ path('typePapier',{id:t.id}) }}"><i class="text-info fas fa-pencil-alt"></i></a></td>
            <td class="px-5"> <a onclick='return confirm("Are you sure you wanna delete this type?")'
                                 href="{{ path('delete',{ repository: 'Typee',id : t.id,redirect: 'typePapier' }) }}"><i class="text-danger fas fa-trash-alt"></i></a></td>
        </tr>
        </tr>
        {% endfor %}

        </tbody>
    </table>
    {{ form(typeeForm) }}
{% endblock %}